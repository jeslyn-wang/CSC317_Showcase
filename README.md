# Name of your Piece

## Personal Information
- Full Name: [Your Full Name]
- UtorID: [wangjesl]
- Student number: [1007915838]
- Assignment augmented: [A2]

## Instructions
1. Run python thingy and then cmake stuff blah blah blah

Replace commands above with your project's actual commands.

## Description
Short summary of what was produced and how it extends the referenced assignment.

Explicit list of features added and code locations:
- Feature: Interactive controls (keyboard/mouse)  
  - Applies to: input handling and UI state  
  - Location: `src/input.js` or `static/js/input-handler.js`
- Feature: New visual effect / animation (e.g., shader, particle system)  
  - Applies to: rendered Piece visuals and frame updates  
  - Location: `src/render.js`, `shaders/` or `static/js/renderer.js`
- Feature: Responsive layout and UI overlay (instructions, score)  
  - Applies to: on-screen HUD and CSS rules  
  - Location: `src/ui/` or `static/css/styles.css`
- Feature: Asset loading (images, audio, models) with caching/error handling  
  - Applies to: startup/boot sequence and runtime resource management  
  - Location: `src/assets.js` or `static/js/loader.js`
- Feature: Performance optimizations (requestAnimationFrame, throttling)  
  - Applies to: main loop and event listeners  
  - Location: `src/main.js` or `static/js/app.js`

Notes:
- For each feature above, mention specific functions or classes you modified, e.g., `Renderer.drawParticles()`, `Input.onKeyDown()`, `Loader.loadAssets()`.
- If you added or changed data files, list them and their paths (e.g., `assets/soundtrack.mp3`, `assets/model.glb`).

## Acknowledgements
- Libraries used (replace with actual libs and versions):
  - p5.js — https://p5js.org/ (MIT)
  - three.js — https://threejs.org/ (MIT)
  - React — https://reactjs.org/ (MIT)
  - Other: Lodash, Howler.js, Tone.js, etc.
- Assets:
  - Images / icons: specify source and license, e.g., "Icon by [Author] from [source] — License"
  - Sounds: specify source and license
  - Fonts: specify font family and license
- Tutorials, references, or code snippets adapted:
  - Include full citations or links and their licenses
- If you used course materials or code examples, cite them here.

Fill in the placeholders with your actual names, commands, paths and citations.